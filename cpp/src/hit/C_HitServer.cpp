#include "hit/C_HitServer.h"

#include <godot_cpp/core/class_db.hpp>

using namespace godot;

void C_HitServer::_bind_methods() {
	BIND_ENUM_CONSTANT(RECT_EDGE_BACK);
	BIND_ENUM_CONSTANT(RECT_EDGE_FRONT);
	BIND_ENUM_CONSTANT(RECT_EDGE_LEFT);
	BIND_ENUM_CONSTANT(RECT_EDGE_RIGHT);

	BIND_CONSTANT(UNLIMITED_HITS);
	BIND_CONSTANT(NO_PREFERRED_TARGET);
	BIND_CONSTANT(NO_CURVE);
	BIND_CONSTANT(BUFFER_MIN_CAPACITY);
	BIND_CONSTANT(CURVE_SAMPLE_COUNT);
}
